function deluser()
{
    var deleteuser = prompt("enter name of the user to delete");
    if (deleteuser != null){
        confirm("Press ok to confirm delete of  " + deleteuser + "username");
    }   
}